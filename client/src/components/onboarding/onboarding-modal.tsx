import React from 'react';

/**
 * OnboardingModal is now completely disabled per user request.
 * The onboarding modal should not appear for logged-in users.
 */
export const OnboardingModal: React.FC = () => {
  // Disabled component that does nothing
  return null;
};